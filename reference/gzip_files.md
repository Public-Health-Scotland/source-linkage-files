# Compress CSV with gzip

Compress CSV with gzip

## Usage

``` r
gzip_files(year = NULL, path = get_year_dir(year, extracts_dir = TRUE))
```

## Arguments

- year:

  The Financial Year e.g. 1718

- path:

  Specify a path to recurse over - Default is to use `year` to get
  extract directory.
