# Select linking ID Set linking_id as anon_chi if available otherwise social_care_id with sending location.

Select linking ID Set linking_id as anon_chi if available otherwise
social_care_id with sending location.

## Usage

``` r
select_linking_id(data)
```

## Arguments

- data:

  social care data with anon_chi and social_care_id with
  sending_location

## Value

social care data with anon_chi and social_care_id with sending_location
