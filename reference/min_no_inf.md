# Custom minimum

Custom minimum function which removes missing values but doesn't return
Inf if all values are missing (instead returns NA)

## Usage

``` r
min_no_inf(x)
```

## Arguments

- x:

  Vector to return min of

## See also

Other helper_funs:
[`max_no_inf()`](https://public-health-scotland.github.io/source-linkage-files/reference/max_no_inf.md)
